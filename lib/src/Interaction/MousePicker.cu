#include <Interaction/MousePicker.cuh>

void 
Interaction::pickMouse(const uint32_t x,
                       const uint32_t y,
                       Scene& scene,
                       Camera& camera,
                       Geometry* outGeometry)
{

}